# DeepSeek 配置（可选）。把本文件复制为 .env 并填入你自己的密钥。
# 未配置 DEEPSEEK_API_KEY 时，程序使用离线模拟大脑，全链路依然可以跑通。
#
# 注意：请把密钥写进 .env（已在 .gitignore 中），不要写在本文件里 —— 本文件是入库的示例。

DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com