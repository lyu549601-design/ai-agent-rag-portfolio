"""可信采购 Agent（Trusted Procurement Agent）。

一个用于求职演示的 "会治理 + 会评测" 的采购 Agent：

* 只读操作自动执行；
* 任何 "写 / 改" 操作都必须经过人工审批；
* 工具输出视为不可信内容，先过提示注入检测；
* 所有关键动作写入 hash-chain 审计日志，可离线校验；
* 内置 golden 评测集与门禁，输出真实评测数字。

数据全部为脱敏样例，不包含任何真实公司数据。
"""

__version__ = "0.1.0"
__all__ = ["__version__"]